module handles.foreachlist;

import std.stdio;
import std.array;
import std.string;

void forEachList()
{
    import std.algorithm.iteration : map, reduce;

    auto firstList = [0];
    auto restLists = [1];

    auto mappedList = restLists;
    auto reducedList = firstList;

}
